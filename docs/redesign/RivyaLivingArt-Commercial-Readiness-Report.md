# Current status addendum — 24 September 2026

The Phase 10 report below is historical. Pro eligibility, shared content publication, restricted-runtime preparation, retention controls, recovery evidence and partial Phase 11 checks have progressed. The owner now requests publication of completed work and explicitly stops further development/testing. Read PENDING-WORK.md for all remaining findings and the release receipt for publication results. This overrides the historical release hold for the current requested deployment; it does not mark unresolved findings complete.

---

# RivyaLivingArt — Commercial Readiness Report

**Phase 10 · 24 September 2026 · Master revision 3.11**

**Decision: audit/remediation checkpoint complete; commercial release HELD.**

This is a source and operational audit, not final acceptance, a legal certification or a production deployment. Phase 9 is merged in main at `80be3a0120e200b2d7ce874305b2d117d4e104e1` (PR #24). This work continues it on `codex/phase-10-commercial-readiness`. The complete approved plan remains in force; no repeated plan, asset or shared-storage approval is needed.

## 1. What currently conflicts with commercial use

The confirmed service restriction is **Vercel Hobby hosting this commercial business**. The authenticated project dashboard still shows Hobby. Vercel's current documentation restricts that plan to non-commercial personal use. Removing payments, using manual WhatsApp, or calling the website a catalogue does not establish an exception. The owner's free-only/Vercel-only decision therefore continues to hold production. No trial, paid plan, Netlify deployment or replacement hosting was introduced. [Vercel Hobby plan](https://vercel.com/docs/plans/hobby).

The saved-order form, Studio, Kanban, private reference handling and manual WhatsApp handoff are not themselves identified as license violations. They remain the approved business workflow. There is no WhatsApp Business API, bot, marketing, support chat, automated sending, customer account or payment gateway to remove.

The current lockfile declares licenses for all 616 dependency entries. Direct dependencies declare MIT, Apache-2.0 or ISC. Transitive dependencies also include LGPL, MPL, BSD, CC-BY and other licenses with obligations; **commercial permission does not mean attribution or redistribution obligations disappear**. This metadata inventory is not a complete review of every bundled license/notice. Preserve upstream notices and complete the release-artifact review before distribution. Font OFL texts and original source/hash manifest are already retained. No non-commercial-only dependency declaration was found in this inventory. See `phase-10-license-inventory.json`.

Owner-supplied imagery remains authorized by the owner's prior approval. The website describes generated imagery as design visualizations; it must not imply that these are photographs of completed commissions. Eight fictional portfolio examples remain unpublished. No new assets were generated in Phase 10.

## 2. Phase task dispositions

| Task | Work performed | Result and limit |
|---|---|---|
| P10.1 Hosting/account/environment | Confirmed team/project through connector; authenticated Vercel dashboard; official Hobby terms; environment names/scopes | Correct project; Hobby commercial eligibility unresolved. No settings changed or secrets revealed. |
| P10.2 Authorization/data lifecycle | Reviewed login, session, role/assignment checks, validation, order transaction, handoff, reference upload/read/cleanup, settings and exports | Four source protections repaired; lifecycle/grant/runtime proof remains gated below. |
| P10.3 Database/recovery | Read-only transactions against the saved shared target; table counts, columns, constraints and effective role capabilities; private-object inventory; encrypted-backup file presence | Sixteen tables, 134 columns, 151 constraints; one deliberately unvalidated legacy-provider check. Overprivileged credential and unproved full recovery are release blockers. |
| P10.4 Truthful content/publication | Reviewed published-only projections, Phase 8 copy, Phase 9 association corrections and current empty database | No publication performed. Policies, complete image-subject review and deliberate publication still required. |
| P10.5 SEO/accessibility/performance | Source review of indexing/canonicals/robots/private headers, dialog/focus/labels, reduced motion, image/font/loading boundaries | Source safeguards retained. Measured and assistive-technology evidence remains Phase 11; no score or conformance claim. |
| P10.6 Monitoring | Added admin-only aggregate service/attention view and free manual operating procedure | No external analytics, personal-data events, paid alerts or WhatsApp notifications. Runtime proof pending. |
| P10.7 Findings/remediation | Source fixes, explicit findings/owners/next actions, refreshed operational and continuation documentation | Release remains held under the plan's exit condition. Next P11.1 starts safe local QA; shared resources remain prohibited for synthetic tests. |

The standing development-first instruction keeps formal lint/build/unit/browser/device/API/restore QA in Phase 11. Read-only operational evidence and TypeScript development wiring are not a substitute.

## 3. Fresh evidence and preservation

`phase-10-live-audit.json` contains a timestamped, sanitized receipt. The connection from the ignored local environment exactly matched the previously saved shared database connection in memory; no connection string was printed. Every database query ran in a read-only transaction. Private Blob listing returned zero objects and no continuation. No customer rows, references, passwords, session tokens or backup contents were published.

- All 16 table counts match the earlier configuration receipt: only the existing storage-budget row and Studio-session row are present. All orders, inquiries, staff, catalogue, public media, content, revisions, audit and business-settings tables are empty.
- All 16 tables grant the inspected role owner access and TRUNCATE; the role also has create-role, create-database, bypass-RLS and schema-create capabilities. It is not a PostgreSQL superuser. These are observed privileges, not evidence of a successful exploit.
- The one unvalidated constraint is `rivya_references_vercel_only`, intentionally created `NOT VALID` to preserve unknown historical-provider records. The references table is empty. This is not evidence of existing invalid data. Validate only through a separately reviewed migration after reconciling the current inventory.
- Both encrypted database archives, encrypted inventory files and encrypted rollback-connection files are present under the existing local backup ID `shared-database-2026-09-23T15-25-17-543Z`. Previous receipts prove encryption roundtrip and archive listing; Phase 10 confirms presence only. No full restore was performed.
- Authenticated Vercel environment metadata lists separate Production and Preview session-secret entries, database entries and the shared private Blob token/store scopes. Secret quality/equality and effective deployed connections were not read back. Searching `RIVYA_ORDER` returned no project variables; source defaults disable both order-write flags. Local flags explicitly remain false.
- The dashboard and connector were read-only. No migration, grants, credential rotation, seed, draft save, content publication, private export/deletion, message sending or deployment occurred.

Saved project environment settings affect future deployments. Existing deployment settings are not retroactively changed. Integration resource labels can still reference the old database association; they do not prove a connection's decrypted destination. Keep the independent runtime-target check as a release gate.

The dashboard independently identifies the unchanged production deployment as `dpl_7HRC8CVEJ98ih86yRxoppxVgMgR2`, source `f9533bbbaf3cc2843025f1a1243442b0a9d920e8`. Its provider Ready label is not verification of this newer candidate. See `phase-10-provider-evidence.json`.

## 4. Findings register

“Source fixed” means implemented and compiler-checked, with regression/role/browser verification still required in Phase 11. Open High findings all block activation. Medium findings marked as release gates must also be resolved or receive a documented, justified disposition before final release.

| ID / severity / area | Issue and evidence | Commercial impact | Fix, files/components and status |
|---|---|---|---|
| CR-01 **High** Hosting | Fresh project dashboard shows Hobby; official plan permits non-commercial personal use only | Current free-only/Vercel-only combination has no established commercial release path | **OPEN / RELEASE HELD.** Owner/provider must establish eligible no-cost commercial permission or change the constraint. Do not purchase, deploy or assume a loophole. `vercel.json`, hosting report. |
| CR-02 **High** Runtime grants | Live audit observes ownership, TRUNCATE, role/database creation and RLS bypass | An application credential compromise can affect schema and all business records | **OPEN.** Prepare a distinct restricted runtime login using the grant matrix in `PHASE-10-OPERATIONS-RUNBOOK.md`; prove it in disposable resources before changing shared application credentials. Keep migration credentials separate. No live grant changes made. |
| CR-03 **High** Recovery | Independent encrypted archives exist; only previous archive listing/roundtrip, no full restore | Recoverability and recovery time/data-loss tolerance are unproved; Windows-profile loss can prevent decryption | **OPEN.** Restore database plus reference manifest/objects into a different disposable destination, revoke restored sessions, prove counts/invariants and private reads. Establish portable/off-device encrypted recovery and recovery objectives. Runbook and shared-data receipt. |
| CR-04 **High** Personal-data lifecycle | Submitted inquiry/reference/notes/history/export/backup retention rules are unconfirmed; no submitted-data erasure workflow | Cannot promise or carry out a complete, evidenced lifecycle across all copies | **OPEN.** Obtain exact previously requested operating facts, implement scoped identity-verified erasure with durable reference-deletion recovery, audit minimization and backup expiry; test isolated. No guessed deadline or automatic deletion added. `reference-cleanup.ts`, migration/runbook privacy map. |
| CR-05 **High** Durable publication | Fresh DB has zero catalogue, media, content and business-settings rows | A successful deployment would not deliver the complete approved catalogue or order destination | **OPEN.** Complete image/policy review, then deliberately publish media before products, reviewed copy and business details. Preserve saved versions; never import the old candidate wholesale. `shop-catalogue.ts`, `published-content.ts`, Studio editors. |
| CR-06 **High** Integrated proof | No exact-candidate formal QA has run | Authorization, transactions, accessibility, devices and failure recovery remain unproved | **OPEN.** P11.1–P11.6; execute local checks first and use separate disposable DB/Blob for runtime cases. No production/shared synthetic records. |
| CR-07 **High** Login throttling | Authentication trimmed login IDs but account throttle did not; whitespace variants created different throttle keys | Distributed attempts could bypass the per-account cap, although the per-IP cap remained | **SOURCE FIXED.** Both use the same trimmed identity; throttle also lowercases consistently with staff lookup. Owner case-sensitive authentication remains unchanged. `src/app/studio/login/actions.ts`. Prove whitespace/case/IP variants and expiry in isolated QA. |
| CR-08 **Medium** Published settings | Database JSON was type-cast without runtime validation or field projection | Corrupt contact data/extra properties could cross the public boundary | **SOURCE FIXED.** Validate existing saved row and positive version, return only four public fields; malformed saved rows fail closed. Missing row retains approved defaults with version zero, not a published claim. `business-settings.ts`. |
| CR-09 **Medium** Product publication | Product validation allowed an approved image belonging to another product; public reader then hid it | Studio could report publication success while the public piece disappeared or lost imagery | **SOURCE FIXED.** Publishing requires valid published media owned by that product for primary, scene and gallery; draft saving remains available. `api/studio/workspace/route.ts`. Reader still revalidates ownership after later changes. |
| CR-10 **Medium** Operational visibility | Settings used hardcoded contacts and environment-presence booleans | Staff could confuse a configured credential with an operational service | **SOURCE FIXED.** Real DB aggregate read, published-contact state, explicit storage-not-checked status, intake/message/indexing state and bounded attention counts. Admin-only API; no public health route. `service-status.ts`, operations API/UI. Runtime proof pending. |
| CR-11 **High** HMAC configuration | Guest/rate-limit hashing accepted any nonempty secret, weaker than Studio's minimum | An incorrectly configured short key could weaken guest ownership/rate-key protection | **SOURCE FIXED.** Require at least 32 characters at the hashing boundary as well as Studio setup. Existing weak-secret guest receipts would become unavailable; verify/rotate through recovery-aware configuration procedure before activation. `request-security.ts`. Randomness still requires operational verification. |
| CR-12 **Medium** Schema status | One legacy-provider constraint remains NOT VALID; old script header said migration unapplied | A future operator could replay applied DDL or overstate historical validation | **DOCUMENTATION FIXED; VALIDATION OPEN.** Active warning points to applied Phase 6/shared receipts. Reinspect then review `VALIDATE CONSTRAINT`; never relabel or delete legacy objects. `phase2-order-contract.sql`, migration runbook. |
| CR-13 **High** Environment/runtime evidence | Future shared scopes are recorded; active deployments have captured settings; runtime grant and independent-secret proof absent | Wrong data destination or reused credentials can undermine isolation and rollback | **OPEN.** Compare nonsecret target identities per future deployment, independent session secrets and secure cookies; keep QA wholly separate. No full secret values in logs/reports. `SHARED-DATA-CONFIGURATION.md`. |
| CR-14 **High** Business policies | Exact retention, delivery/installation charges/areas and change/cancellation rules are still unanswered | Public policy wording cannot claim facts not established by the owner | **OPEN.** Carry the existing specific question forward; do not request blanket reapproval or invent terms. Publish final reviewed policies only when facts are settled. Phase 8 proposals, privacy/terms/delivery/returns documents. |
| CR-15 **Medium** Media/claims | Fifteen images inspected in Phase 9, six corrected associations; remaining subjects and DP118 edge treatment need review | Wrong imagery or visualization presented as completed work misleads customers | **OPEN / RELEASE GATE.** Check all 120 products/36 articles and device crops; apply six corrected media/product records deliberately. Preserve original bytes and fictional-project exclusion. Phase 9 manifest/corrections and instance register. |
| CR-16 **Medium** License obligations | License declarations cover 616 lockfile entries; native image dependencies include LGPL and transitive packages include MPL/CC-BY | Permission to use commercially does not replace notices or source/redistribution duties | **OPEN / RELEASE GATE.** Review actual release bundle, retain required license/notice/source information, record dispositions for copyleft/attribution dependencies. Inventory is factual metadata, not clearance. No dependency replacement needed merely because it permits commercial use with obligations. |
| CR-17 **Medium** Measurement/SEO | Source guards exist; no measured performance, contrast, screen-reader or full route proof | Fast/accessible/search-ready claims would be unsupported | **OPEN / RELEASE GATE.** P11 measurements, metadata/canonical/domain checks, private noindex/header checks and full route/device matrix. No indexing activation now. |
| CR-18 **Medium** Operational retention | Rate-limit keys can accumulate; submitted evidence has no defined deletion cycle; local drafts are tab memory | Storage growth and inconsistent recovery expectations | **OPEN.** Define bounded expired-throttle housekeeping and lifecycle policy, preserve active counters and business evidence, then prove cleanup/retry. Do not conflate 24-hour receipt expiry with deletion. `request-security.ts`, Studio recovery and cleanup. |
| CR-19 **Medium** Handoff recovery | Message preparation stops after five attempts; a ready message never proves a customer sent it | Repeated provider/configuration failures need a human recovery path | **RUNBOOK ADDED; RUNTIME PROOF OPEN.** Review saved record and destination, retain immutable brief, investigate five-attempt exhaustion; do not blindly reset counters or resubmit. Never infer Sent/Read/Paid or automatically message anyone. `order-handoff.ts`, operations runbook. |
| CR-20 **Medium** Staff operating dates | Follow-up due dates use database CURRENT_DATE; operating timezone is not confirmed | Staff may see different due-day boundaries near midnight | **OPEN.** Confirm operating timezone, document or implement consistent date semantics, then test boundaries. Private CSV dates remain explicitly UTC. `api/studio/operations/route.ts`. |

## 5. Authorization and workflow review

| Boundary | Source evidence retained | Required Phase 11 proof |
|---|---|---|
| Studio login/session | scrypt password derivation, opaque hashed session token, eight-hour lifetime/30-minute idle, current account/version/active check, secure HttpOnly strict cookie, durable throttles | Wrong credentials, canonical throttle identity, password/role revocation, expiry, outage, logout and cross-environment rejection |
| Editor versus administrator | Inquiry/detail/reference/history/actions recheck assignee; admin-only publish/staff/settings/export/cleanup; editors draft public content | Two editors, reassignment during request, all route/action denial paths, no private record leakage |
| Product/bespoke submission | Strict server schema/visible required fields; canonical idempotency identity; published product/config locking; atomic inquiry/order/references/event/audit transaction | Tampering, stale schema/destination, double submit, concurrency, commit failure and unchanged immutable evidence |
| WhatsApp | Saved v2 snapshot only; separate bounded finalization; destination/version recheck; full-message copy/long-link recovery; manual customer Send | Message generation failure after successful save, retry without duplicate order, long URL/clipboard/popup/browser failure; no automatic Send |
| Private references | Guest-owned upload/session identity, bounded MIME/size/pixels, JPEG normalization, private random path, lease/digest/quota, authenticated assigned/admin stream | Spoofed MIME, decompression bound, interrupted upload, wrong owner/staff, private cache headers, cleanup/commit races and restore |
| Exports and errors | Admin-only scoped export up to 5,000 rows, formula neutralization, no reference URLs, no-store, generic errors | CSV formula cases, scope/date limits, session expiry, partial provider outage, no PII in application/provider error logs |
| Public projection | Published-only catalogue/content/media, exact ownership, canonical redirects, real-project approval filter, no customer data projection | Every instance and reserved alias; empty/hidden/unavailable behavior; no fictional testimonials/orders/projects reachable |

No automated telemetry of customer brief text, phone, email, uploaded material, message URL or WhatsApp destination is introduced. Counts in settings are private operational indicators and do not prove delivery or business outcomes.

## 6. Operations and resumption

Read `PHASE-10-OPERATIONS-RUNBOOK.md` for maintenance/recovery, grant matrix, private-data map, monitoring and publication order. The old Phase 2 migration document is retained as historical design with a current override; it is not an instruction to replay applied migrations.

Next **P11.1**: fetch current Git refs, retain this finding register, run the local final-QA commands on an exact candidate. Before any database/Blob synthetic tests, provision/verify different disposable resources and independent credentials. Free local/offline verification can proceed while CR-01 holds deployment. Repair failures without weakening assertions; update evidence after changes. CR-02/03/04/05/06/13/14 and all required Medium gates must close before activation. P12/main release is not authorized by a partial QA result.

Source verification for this checkpoint: Node 22 TypeScript no-emit completed successfully; readonly provider/schema/count inspection completed. React source review kept privileged reads behind the administrator check, passed only public contact fields/aggregate counts to clients, and added no background polling or expensive client dependencies. Initial approved contact defaults now have an explicit review/publish action even when the values are unchanged; the confirmation step is retained. Formal lint/build/unit/browser/device/API/load/restore QA did not run. Original assets, existing drafts, customer data, historical source candidates and deployment settings are preserved. Git publication receipt follows in `phase-10-github-publication.json`; the local completion artifact records the final documentation SHA without a self-referential commit claim.
