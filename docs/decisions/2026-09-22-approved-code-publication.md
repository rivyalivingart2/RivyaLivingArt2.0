# Owner-directed publication of prepared code — 22 September 2026

The previous checkpoint explicitly told the owner that the target GitHub repository
was public and publishing the private source/media payload remained blocked. The
owner then instructed: “Put this updated code into gitrepo” and supplied
https://github.com/rivyalivingart2/RivyaLivingArt2.0.git, selecting the GitHub plugin.
That instruction authorizes the scoped prepared code and included approved asset
derivatives to this public repository. It does not authorize main merge, Site/Vercel
manual deployment, sharing expansion, secret/domain change or additional Drive access.

Target branch: `codex/sites-approved-design`. The prepared implementation checkpoint
is local `918a017096d8e5bc690094c9ff3c3bcecc5aa66e`; its four local development commits
are retained. Native saved Site Version 5 is source
`d3337a2eac823ff50d4426b7d11c9e9a1a66916d`. Live approved Site Version 1 is unchanged.

Remote inspection finds main at `79f5834dc4028cb42fef4020651ccaac40c16285` (PR #12,
R8-4B merge). Its tree is identical to development base `29250ee0b8d011835a3053510f7d4ea7ca58cf1e`.
Both development branches are still at that base. Publish on top of latest main history
through the authorized GitHub connector; do not overwrite main or rewrite a published branch.

Terminal write credentials are unavailable in this workspace. The repository workflow
permits the GitHub connector's Git Data operations. Upload reviewed blobs, construct
an exact matching Git tree, create a commit based on latest main, then fast-forward
only the named development ref. Server-created commit metadata may differ from local
authoring commits; record local and remote identities separately and verify tree equality.
No minified bundle, HTML scrape, iframe, substitute scaffold or new catalogue is used.

Scope: native source under `experiments/sites/rivyalivingart-studio-preview/`, compatible
existing Next.js public/Studio components/routes/contracts, source fixtures, scoped styles,
genuine dependency locks, eleven exact approved WebP derivatives, six OFL font files,
licenses and source checkpoints. No source credentials, customer records, original Drive
media collection, node_modules or build output is included. Existing preview guards,
tests and deployment configuration remain. Formal QA is deferred by owner instruction.

Read-only Vercel inspection still reports Node 22.x and Vercel Authentication protection
`all_except_custom_domains`; main has production deployment history. A development ref
update may invoke the existing protected Preview integration. No deploy operation or
project-setting mutation is part of this task.

Publication verification is pending in this authoring checkpoint. Read the actual remote
branch and commit tree after updating the ref; do not claim local commit IDs were pushed
unchanged when the connector created an equivalent commit with new metadata.
