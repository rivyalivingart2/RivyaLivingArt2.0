# R8-1 dependency and runtime verification

Date: 21 September 2026. Branch: `codex/r8-first-frontend`.
Starting commit: `3bb86481fc261d6fd17ef59c56884e76d1e5a3b6`.

## Outcome

The existing application now has an actual npm dependency tree and reviewed
`package-lock.json`. This continuation did not scaffold another application.
Installation, lint, semantic typecheck, source/unit tests, preflight tests and the
Next production build passed. The real built server was started and tested over
HTTP. Hydrated browser/layout verification remains blocked, so R8-1 is not UI_READY
and R8-5/V1 is not reached. No R8-2–4 completion or backend work is claimed.

## Dependencies and clean installation

- Verification toolchain: Node **22.23.2**, npm **10.9.2**.
- The workspace initially exposed Node 24/npm 11. Node 22/npm 10.9.2 were installed
  outside the repository through the normal npm registry. Installation was rerun
  with the project toolchain before checks and clean-install verification.
- Root dependency pins are unchanged: Next 16.3.5, React/react-dom 19.3.0,
  Tailwind/PostCSS plugin 4.3.3, TypeScript 5.9.3, ESLint 9.39.1 and
  eslint-config-next 16.3.5. Resolved type packages are recorded by the lockfile.
- Lockfile version 3; **439 package entries including the root**. All 438 resolved
  entries have npm registry HTTPS URLs and integrity hashes. Manifest/lock root
  dependency maps match. No local paths, Git sources or alternate registries added.
- `npm ci --fetch-retries=0 --fetch-timeout=15000` passed under Node 22/npm 10.9.2;
  no integrity bypass, `--force`, peer-dependency override or fake resolution.
- The registry marks ESLint 9.39.1 deprecated/unsupported. It still installs and
  the actual lint check passes; no unsupported-major upgrade was bundled into this
  baseline verification. npm's environment proxy warning was not suppressed.
- `npm ls --depth=0` exits 0 with all required pins present. npm also labels six
  installed optional WASM transitive packages extraneous on this Linux platform;
  all six are represented as optional packages in the actual lockfile.

## Fixes and evidence

| Check | Actual result |
|---|---|
| `npm run lint` | Exit 0, no ESLint warnings/errors after naming the PostCSS config and removing two unused imports. No rule disabled. |
| `npm run typecheck` | Exit 0. Now runs `next typegen && tsc --noEmit`; also passed with the old `.next` build moved outside the repo, before a fresh build. |
| `npm test` | 22 passed, 0 failed; policy/fixture/source tests. |
| `npm run test:preflight` | 12 passed, 0 failed. |
| `npm run build` | Exit 0; actual Next 16.3.5 Turbopack production build. |
| `npm run test:runtime` | 29 reported tests passed, 0 failed, including grouping tests; three actual Next production-mode server processes. |
| Desktop/mobile browser, menu keyboard, focus, reduced motion, image decode, client navigation | **BLOCKED / NOT VERIFIED**, not replaced by HTTP or static HTML checks. |

The HTTP suite verifies all existing collection/detail routes, Studio's truthful
holding state, same-origin link destinations, both served AVIF files against their
committed bytes, response content types and noindex headers. It verifies unknown
collection/product/nested routes return HTTP 404, and both local production without
a preview flag and simulated Vercel production with the flag keep fixture pages
closed. Environment-policy cases run only inside isolated local test processes.

Before the fix, unknown collection/product slugs returned HTTP 200 with not-found
content because the root loading boundary had already streamed. The loading view
is preserved as `src/components/page-loading.tsx`, with a homepage-only Suspense
boundary. Collection and product validation can now determine HTTP 404 before any
response is streamed. Future detail loading boundaries belong below validation.
Collection metadata also now uses the preview policy, matching product metadata.

The generated `next-env.d.ts` is retained as generated by the installed Next build.
`npm run check` now includes lint, typecheck, unit, preflight, build and HTTP tests.

Framework references: [not-found response behavior](https://nextjs.org/docs/app/api-reference/file-conventions/not-found)
and [Next type generation](https://nextjs.org/docs/app/api-reference/cli/next).

## Browser and media limitations

The actual app started successfully with `RIVYA_VISUAL_PREVIEW=1 npm start`.
The available Browser Use surface rejected `http://127.0.0.1:3000` with
`net::ERR_BLOCKED_BY_CLIENT`. No browser access controls were changed. An attempted
local agent-browser launch could not start its daemon; the standard Playwright
browser download timed out and was stopped. No successful browser screenshots,
React hydration, mobile dialog, layout or image-decoding evidence is claimed.

Drive intake confirmed both recorded originals are 1122×1402 in the source index.
Riverline's raw download was rejected by automatic approval review for missing
private-source authorization; no raw retry followed. Basin returned an authenticated
file reference, but normal byte transfer returned HTTP 403 (`error code: 1010`).
No original bytes were obtained or freshly inspected, no signed URLs are recorded,
and no replacement derivatives were created. Both existing small AVIFs and the
original provenance manifest remain unchanged.

## Publication, scope and next task

The checkout started clean on the exact owner-specified commit. Origin fetch/push
identity matches `rivyalivingart2/RivyaLivingArt2.0`, with no separate push URL.
The remote branch still matched the starting SHA before publication. No repository
deployment workflows/configuration were found. The connected Vercel team returned
zero projects again; this observation does not cover unrelated accounts.

Terminal push failed because no GitHub username/write credentials are available.
Publication uses the authorized GitHub connector instead; verify its actual branch
ref and committed tree. Connector commit metadata may give a different SHA from
the unpublished local commit; tree equality is checked before the branch update.
Commit/push verification is pending for this checkpoint commit; record the actual
published SHA in the session result, not as a predicted self-reference here.
Main, production, domains, Vercel settings, all nine briefs and historical evidence
documents remain unchanged. Two fictional source products remain; zero database
records. No secrets, staff access, uploads, messages or backend integrations added.

**SOURCE_IMPLEMENTED:** dependency/runtime repair slice. **UI_READY:** no.
**BACKEND_CONNECTED:** no. **TESTED:** build/unit/preflight/HTTP as above.
**R8-5/V1:** not ready; no preview deployment exists.

**One exact next task:** on this branch in an authorized browser-capable environment,
run the real frontend at 390, 768 and 1440 px and verify mobile-menu keyboard/focus/
Escape/scroll restoration, navigation, image decoding, unknown routes and reduced
motion; fix any failures and capture actual screenshots before the R8-2 furniture
typography/media/collection/detail slice.
